PORT=4000
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/vending_pos
JWT_SECRET=change_this_to_a_long_random_secret
JWT_EXPIRES_IN=8h
CORS_ORIGIN=http://localhost:5500

# local for development, supabase for persistent online product photos (Node.js 18+).
PRODUCT_IMAGE_STORAGE=local
SUPABASE_URL=
# Backend secret only. Never put this key in frontend files.
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_STORAGE_BUCKET=product-images
